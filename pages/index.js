import { useRouter } from "next/router";
import { useMatchMedia } from "utils/useMatchMedia";
import { supabase } from "utils/supabase";

import Head from "next/head";
import TitleBar from "@/components/organisms/Index/TitleBar/TitleBar";
import Header from "@/components/organisms/Index/Header/Header";
import Border from "@/components/atoms/Border";
import TimeFilters from "@/components/molecules/Index/PostLoop/TimeFilters";
import PostLoop from "@/components/organisms/Index/PostLoop/PostLoop";
import JobLoop from "@/components/organisms/Index/RemoteJobs/JobLoop";
import PartnerUpLoop from "@/components/organisms/Index/PartnerUp/PartnerUpLoop";
import MeetupsLoop from "@/components/organisms/Index/Meetups/MeetupsLoop";
import Footer from "@/components/organisms/Index/Footer/Footer";

import { jobInfo, partnerInfo, meetupInfo, footerColumns } from "utils/consts";
import Container from "@/components/templates/Container";
import PostLoopDesktop from "@/components/organisms/Index/PostLoop/Desktop/PostLoopDesktop";

export default function Home({ posts }) {
  const isMobile = useMatchMedia("(max-width:1280px)", true);
  const router = useRouter();
  return (
    <div>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main>
        <TitleBar />
        <Container>
          {isMobile ? (
            <div>
              {/* Mobile Layout */}
              <section className="mx-4 mt-10">
                <div className="sm:border-[3px] sm:border-[#1f364d] sm:mt-7 sm:rounded-sm sm:mx-3">
                  <div className="sm:border-b-[3px] sm:border-[#1f364d]">
                    <Header />
                    <Border className="hidden sm:block sm:border-b-[3px]" />
                    <Border className="my-3 sm:hidden" />
                    <TimeFilters className="p-2" />
                  </div>
                  <Border className="my-3 sm:hidden" />
                  <PostLoop props={posts} />
                </div>
              </section>

              <section className="mx-4 mt-10">
                <JobLoop props={jobInfo} />
              </section>

              <section className="mx-4 mt-10">
                <PartnerUpLoop props={partnerInfo} />
              </section>

              <section className="mx-4 mt-10">
                <MeetupsLoop props={meetupInfo} />
              </section>
            </div>
          ) : (
            <div className="grid grid-cols-4">
              {/* Desktop Layout */}
              <section className="mx-4 mt-10">
                <MeetupsLoop props={meetupInfo} />
              </section>
              <section className="col-span-2 mx-4 mt-10">
                <div className="sm:border-[3px] sm:border-[#1f364d] sm:mt-7 sm:rounded-sm sm:mx-3">
                  <div className="sm:border-b-[3px] sm:border-[#1f364d]">
                    <Header />
                    <Border className="hidden sm:block sm:border-b-[3px]" />
                    <Border className="my-3 sm:hidden" />
                    <TimeFilters className="p-2" />
                  </div>
                  <Border className="my-3 sm:hidden" />
                  <PostLoopDesktop props={posts} />
                </div>
              </section>
              <div>
                <section className="mx-4 mt-10">
                  <JobLoop props={jobInfo} />
                </section>
                <section className="mx-4 mt-10">
                  <PartnerUpLoop props={partnerInfo} />
                </section>
              </div>
            </div>
          )}
        </Container>
      </main>
      <div className="my-12 md:border-t-[3px] sm:border-[#1f364d]" />
      <Container>
        <footer>
          <Footer props={footerColumns} router={router}></Footer>
        </footer>
      </Container>
    </div>
  );
}

export async function getStaticProps() {
  const { data } = await supabase.from("posts").select();
  return {
    props: {
      posts: data,
    },
  };
}
